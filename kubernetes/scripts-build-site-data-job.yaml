apiVersion: batch/v1
kind: Job
metadata:
  name: scripts-build-site-data
  namespace: club-5060ti
  labels:
    app.kubernetes.io/name: club-5060ti
    app.kubernetes.io/component: scripts
    app.kubernetes.io/managed-by: autopoc
    poc.autopoc.dev/scenario: build-site-data
spec:
  backoffLimit: 1
  activeDeadlineSeconds: 120
  template:
    metadata:
      labels:
        app.kubernetes.io/name: club-5060ti
        app.kubernetes.io/component: scripts
        poc.autopoc.dev/scenario: build-site-data
    spec:
      containers:
        - name: scripts
          image: quay.io/aicatalyst/club-5060ti-scripts:latest
          imagePullPolicy: Always
          command: ["python3"]
          args: ["scripts/build_site_data.py"]
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            capabilities:
              drop:
                - ALL
      restartPolicy: Never
