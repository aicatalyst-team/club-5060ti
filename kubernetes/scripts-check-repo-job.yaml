apiVersion: batch/v1
kind: Job
metadata:
  name: scripts-check-repo
  namespace: club-5060ti
  labels:
    app.kubernetes.io/name: club-5060ti
    app.kubernetes.io/component: scripts
    app.kubernetes.io/managed-by: autopoc
    poc.autopoc.dev/scenario: check-repo
spec:
  backoffLimit: 1
  activeDeadlineSeconds: 120
  template:
    metadata:
      labels:
        app.kubernetes.io/name: club-5060ti
        app.kubernetes.io/component: scripts
        poc.autopoc.dev/scenario: check-repo
    spec:
      containers:
        - name: scripts
          image: quay.io/aicatalyst/club-5060ti-scripts:latest
          imagePullPolicy: Always
          command: ["bash"]
          args: ["scripts/check_repo.sh"]
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            capabilities:
              drop:
                - ALL
      restartPolicy: Never
