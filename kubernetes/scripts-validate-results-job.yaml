apiVersion: batch/v1
kind: Job
metadata:
  name: scripts-validate-results
  namespace: club-5060ti
  labels:
    app.kubernetes.io/name: club-5060ti
    app.kubernetes.io/component: scripts
    app.kubernetes.io/managed-by: autopoc
    poc.autopoc.dev/scenario: validate-results
spec:
  backoffLimit: 1
  activeDeadlineSeconds: 120
  template:
    metadata:
      labels:
        app.kubernetes.io/name: club-5060ti
        app.kubernetes.io/component: scripts
        poc.autopoc.dev/scenario: validate-results
    spec:
      containers:
        - name: scripts
          image: quay.io/aicatalyst/club-5060ti-scripts:latest
          imagePullPolicy: Always
          command: ["python3"]
          args: ["scripts/validate_results.py", "data/results"]
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            capabilities:
              drop:
                - ALL
      restartPolicy: Never
